{
    "widgets": [
        {
            "modulePath": "{{namespacePath}}/{{pluginname}}/plugin/{{pluginname}}",
            "type": "{{namespace}}.{{pluginname}}.plugin.{{pluginname}}"
        }
    ]
}